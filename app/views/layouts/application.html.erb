<!DOCTYPE html>
<html>
  <head>
    <title>Todoリストアプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-gray-100 min-h-screen flex flex-col">
    <% if notice.present? %>
      <p class="bg-blue-100 text-blue-800 border border-blue-200 rounded px-4 py-2 m-2">
        <%= notice %>
      </p>
    <% end %>

    <% if alert.present? %>
      <p class="bg-red-100 text-red-800 border border-red-200 rounded px-4 py-2 m-2">
        <%= alert %>
      </p>
    <% end %>

    <header class="bg-blue-600 text-white shadow-sm">
      <div class="max-w-7xl mx-auto text-center text-2xl font-semibold p-4">
        Todoリスト
      </div>
    </header>

    <main class="flex-grow flex items-center justify-center py-8">
      <%= yield %>
    </main>

    <footer class="bg-gray-100 text-center text-gray-500 py-2">
      © 2025 Todoリストアプリ
    </footer>
  </body>
</html>

